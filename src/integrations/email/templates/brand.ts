// Placeholder brand values — swap for real Kilo brand guidelines (logo,
// exact palette) once design has them; kept in one place so every template
// stays in sync.
export const brand = {
  name: 'Kilo',
  primaryColor: '#4F46E5',
  textColor: '#111827',
  mutedColor: '#6B7280',
  backgroundColor: '#F9FAFB',
  borderColor: '#E5E7EB',
  fontFamily: '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif',
};

// Per-email-type accent — each template picks one to color its icon badge
// and call-to-action so the purpose of the email reads at a glance (a red
// "reset your password" doesn't look like a teal "you've been invited").
export const accent = {
  indigo: '#4F46E5',
  red: '#DC2626',
  teal: '#0D9488',
};
